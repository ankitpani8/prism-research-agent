"""LangGraph state machine: nodes, edges, state, compile, Mermaid export.

Reuse map: M2.1 (typed state, MemorySaver, draw_mermaid) + M3/02 (critic loop).
Phase 2 builds the 4-node graph with MAX_REPLANS. `export_diagram()` lands then.
"""


def export_diagram() -> None:  # pragma: no cover
    raise NotImplementedError("Implemented in Phase 2")
