# Runtime deps. Core versions pinned to Ankit's verified freeze from
# learning_AgenticAI (langgraph 1.x line). New deps (streamlit, search, pdf,
# matplotlib) pinned to current stable; Phase 1 locks ALL of these via
# `pip freeze > requirements.lock.txt` after the provider smoke test passes.

# --- Agent core ---
langgraph==1.2.0
langgraph-checkpoint==4.1.0
langchain-core==1.4.0
langchain-google-genai==4.2.2
langchain-ollama==1.1.0
langchain-anthropic==1.4.3
langchain-community==0.4.1
langchain-text-splitters==1.1.2

# --- Tools: retrieval / search / docs ---
chromadb==1.1.1
onnxruntime==1.20.1   # backs Chroma's default ONNX MiniLM embeddings (no torch)
tavily-python==0.7.14
ddgs==9.6.1
pypdf==6.1.4
pdfplumber==0.11.9

# --- Schemas / config ---
pydantic==2.12.5
python-dotenv==1.2.2

# --- Production API surface ---
fastapi==0.136.1
uvicorn==0.46.0
sse-starlette==3.4.2

# --- Demo UI ---
streamlit==1.42.0

# --- Synthetic data generation ---
matplotlib==3.10.0

# --- Resilience ---
tenacity==9.1.4
httpx==0.28.1
